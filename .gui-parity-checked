5873a9726e0bd34bb8239f560c0ddd219e693b56 2026-07-09 audited f79743a..HEAD (3 commits); 0 gaps remain (2 GUI/CLI default divergences CLOSED).
- This short range is the GUI/CLI default-parity sweep prompted by Andy. Two GUI-only
  fixes to swig_gui.py bring the route tab's defaults into line with route.py + the
  batch_route engine signature:
  * a0bb020 -- "No BGA Zones" box default was "ALL" (-> config['no_bga_zones']=[] ->
    setup_bga_exclusion_zones DISABLES every BGA keep-out); CLI --no-bga-zones defaults
    to None (auto-detect + ENABLE). Changed the control's initial value AND its
    reset-to-defaults value to "" (== None). planes_gui reads the same shared
    no_bga_zones_text and inherits the enabled default. This was a real STARTUP divergence
    (the TextCtrl had an explicit value="ALL").
  * 5873a97 -- reset_params_to_defaults() set add_teardrops / mps_reverse_rounds /
    mps_layer_swap / mps_segment_intersection / can_swap_to_top to True, contradicting the
    checkbox creation state (all created unchecked / add_teardrops SetValue(False)), the CLI
    (--add-teardrops / --mps-* / --can-swap-to-top-layer are store_true => default off), and
    batch_route (all default False). Set the five reset values to False. NOTE this was a
    "Reset to defaults" bug only -- fresh startup already matched the CLI (those controls are
    never set True except by the reset button), so it never silently changed a normal route.
- Wider sweep result (recorded for the next auditor): every numeric "Advanced" control on
  both fronts sources defaults.X from routing_defaults, so they match by construction. Four
  CLI argparse defaults are hardcoded literals rather than defaults.X (--bga-proximity-radius
  7.0, --bga-proximity-cost 0.2, --proximity-heuristic-factor 0.02, --vertical-attraction-
  radius 1.0) -- all currently EQUAL their constants (BGA_PROXIMITY_RADIUS etc.), so no
  divergence today, but a latent drift risk worth pointing at defaults.X eventually.
  ordering_strategy matches ("mps" both sides); direction "Auto" == CLI --direction None;
  move_copper_text is GUI-only (no CLI flag). Diff-pair / planes tabs vs route_diff.py /
  route_planes.py were NOT swept this pass (route tab only).
- b9d345c is the prior parity-file update commit (doc only). No engine parameter/flag/
  results-key was added anywhere in the range.
