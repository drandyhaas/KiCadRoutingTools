066fc7d 2026-07-13 Tier D parity sweep (#381) + Tier E (#382); gaps D1-D9 FIXED.

This is not just an audit — issue #381 (review #368 Tier D) found and FIXED
the CLI/GUI parity gaps D1-D9, so the parity surface changed and was
re-verified end to end. Merged fe7a94d (#381) + 066fc7d (#382).

WHAT #381 CLOSED (the gaps the previous marker listed as "still not swept" —
the diff-pair tab and the fanout advanced knobs — are now swept):
  * D1 differential_gui._run_diff_routing threads the FULL merged config to
    batch_route_diff_pairs (was dropping ~30 params incl. ordering_strategy,
    which made GUI diff order pairs differently from CLI). Added a
    KICAD_DUMP_BATCH_KWARGS dump to batch_route_diff_pairs so the diff path is
    now instrumentable like batch_route.
  * D2 reset_params_to_defaults diff GND-via -> True (matches CLI default-on).
  * D3 polarity-swap control+reset default empty=deny (#279); claude_plan
    alias polarity_swap_nets -> polarity_swap_nets_text.
  * D4 manifest_to_plan aliases route_diff --track-width -> diff_pair_width;
    GUI DIFF_PAIR_WIDTH default 0.1 -> 0.3 (GUI-side, to match CLI).
  * D5 claude_plan resolves rip_existing_nets/impedance/ordering/direction/
    time_matching/keepout/guide_corridor/length_match_groups/swappable_nets to
    their controls; parity gate extended with a param->control resolution check.
  * D6 planes create/repair thread layer_costs + plane knobs; skip_existing_zones
    config-driven, interactive default kept True.
  * D7 QFN fanout GUI controls default 0.1/0.1 (QFN_TRACK_WIDTH/QFN_CLEARANCE).
  * D8 add_drc_fix_args in both fanout parsers; check_drc registered in the
    postpass-coverage lint (run_drc = KNOWN_CLI_ONLY).
  * D9 argparse synonyms (--output on plane scripts, --plane-track-via-clearance
    <-> --track-via-clearance, --no-bga-zones <-> --no-bga-zone).
  DEFERRED (noted, not shipped): route_diff --rip-existing-nets (engine has no
  such param), place_fanout_clearance fab-tier args (would change CLI default).

#382 (Tier E) additions relevant to parity: an AST GridRouteConfig field-parity
gate in run_doc_examples.py; and fixed the per-netclass GUI multi-batch
aggregator dropping vias_to_remove (GUI route + --rip-existing-nets shipped
stale vias).

VERIFICATION (all green):
  * CLI byte-identical: corpus A/B 45 boards (sets 1/2/5) zero-delta on every
    deterministic metric; the one kicad_drc move (urchin 9->10) is proven
    identical copper (same geometry hash = kicad-cli grading noise).
  * test_manifest_plan_parity.py: 6201 flag-checks, 0 mismatches; param->control
    resolution 12/12.
  * test_cli_postpass_coverage.py: 0 failures.
  * test_gui_engine_parity.py (KiCad python, splitflap): VERDICT PARITY -- CLI
    and GUI fully connected, 0 DRC, 0 kicad-unconnected; copper differs only by
    the documented A* tie-break/UUID class.

STILL NOT DIRECTLY GUI-ENGINE-TESTED (structurally covered by the no-wx gates,
but the KiCad-python test only ran splitflap = single-ended + planes): a diff
route through the GUI engine exercising D1's threaded ordering/rip-up/costs, and
a QFN GUI fanout at the D7 defaults. Next audit picks up from this marker.
