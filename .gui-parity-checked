280009bdfb4c25bae980451ab8fcebbbed3c00ac 2026-07-09 audited d5a9d90..HEAD (30 commits); 0 gaps needing fixes.
- Most of the range IS GUI-side work (claude tab: status mirror/relocation, model aliases,
  Save/Load plan + .json wildcard, plan-install defaults reset incl. tab panels + fanout,
  any-parameter mapping with aliases/special fields, per-step live DRC floors, oracle
  Log-tab visibility, added_seg fix, plan log symmetry) or dual-front features built for
  parity (in-engine reconciliation for return_results, in-memory repair cleanup +
  self-reconnect + 6-tuple strip channel with the planes-applier unpack updated in the
  same commit, repair-only oracle both fronts).
- Shared-engine changes ride both fronts automatically: outline-over-credit closure
  (zone_credit_validator + make_real_fill_validator in check_connected, wired through
  every removal gate in pcb_modification), base-disconnected-component protection
  (b696305), tap protected_pads (inside the route_disconnected_planes engine fn),
  oracle rework (zone-kind expansion, fill-flow margins, two-short-links, drill guard),
  GND-stitching fixes in add_gnd_vias (ring shortfall, grid snap).
- VERIFIED parity details:
  * GUI stitching already used ALL board copper layers (_get_all_copper_layers), so the
    5c4a9f8 route_planes all-layers fix brought the CLI UP TO the GUI's behavior;
  * route_planes' create-front cleanup_plane_taps_grazing call passes no protected_pads
    -- covered by the generic gate-level base-disconnected protection (defense layer the
    repair front has in addition);
  * changed signatures (_safe_prune_net zone_credit_validator, cleanup_plane_taps_grazing
    protected_pads) default-compat: external callers (tests, route_planes) unchanged pass.
- CLI-only by design: KICAD_DUMP_BATCH_KWARGS probe (env-gated), manifest_to_plan.py
  (produces GUI-loadable plans -- the tool exists to CROSS the fronts),
  route_planes --no-kicad-recheck flag removed (its consumer was removed in-range).
