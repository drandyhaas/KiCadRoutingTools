d5a9d90cc1170ba2c8c1c4d057bde47bc1856493 2026-07-08 audited 767ef1e..HEAD (14 commits); 0 gaps needing fixes (1 GUI fix landed in-range).
- Shared-engine, both fronts automatically: region-join fill-cell seeding/closest-approach/
  budget-escalation/validity (8a967d3/602c7d9/d5a9d90), cleanup-pass hardening + collapse
  copper-component gate + trim_dangles connectivity gate (763736d), phase-3 synthetic-result
  acceptance (2c9c64e), checker fixes (763736d/ae0f614).
- VERIFIED parity-critical details:
  * exact custom-pad geometry in check_connected._point_in_pad depends on pad.polygons --
    build_pcb_data_from_board populates it via GetEffectivePolygon (kicad_parser.py:2953),
    so GUI-built boards get the same exact-shape grading as parsed files;
  * orphan-island via strips flow to the GUI: cleanup input_strip_vias -> route.py
    stale_input_vias merge (route.py:~1147) -> results_data['vias_to_remove'] (route.py:1444)
    -> swig_gui.py:3091 removal loop.
- GUI-side fix landed in-range: board_swaps handles #340 'added_seg' connector mods by
  creating the track (was KeyError('start') aborting apply midway) (2c9c64e).
- Deliberate CLI-only (documented): kicad-oracle reconnect + --no-kicad-recheck flags on both
  plane fronts (597943c..993a5f7, d5a9d90) -- the GUI runs inside pcbnew where real fills are
  native; a future GUI equivalent should use them directly, not shell kicad-cli. route.py
  end-of-run reconciliation remains guarded off for return_results (GUI contract unchanged).
- No GUI action needed: JSON_ORACLE summary lines and the kicad-cli-absent determinism NOTE
  (CLI stdout only); stub_layer_switching h2h now reads config.hole_to_hole_clearance with a
  0.2 fallback (GUI configs lacking the field degrade to the old constant, no wiring needed).
