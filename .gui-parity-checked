f2dcd12 2026-07-12 audited 8b41e5a..HEAD (18 commits); 0 gaps found.

ADDENDUM (post-audit, same day): the #129 same_net_escapes design this
audit verified was REPLACED by the parameterless escape-priority redo
(the commit FOLLOWING this marker; history squashed). No CLI flag, no
GUI control, no engine kwarg -- the parity surface is unchanged and the
no-wx gates were re-run green after the redo. The same_net_escapes
threading notes below describe the audited-then, now-removed design;
the next audit picks up from this marker and covers the redo commit.

Small range, one new engine param. Method: git log/diff over the range on the
CLI scripts + kicad_routing_plugin/, classify every hunk by location (engine
function vs CLI main() vs GUI-only), then the three gates.

NEW ENGINE PARAM in range: same_net_escapes (--same-net-escapes, #129
ef4b285). Threaded through ALL surfaces at introduction time and re-verified
here per the CLAUDE.md checklist:
  * argparse default 1 == engine default == defaults.BGA_SAME_NET_ESCAPES ==
    GUI spin control initial == reset_params_to_defaults value.
  * fanout_gui BGAOptionsPanel control named same_net_escapes; get_config;
    engine call passes it; settings_persistence save/load.
  * claude_plan fanout/bga branch applies params["same_net_escapes"].
  * manifest_to_plan FLAG_PARAMS carries --same-net-escapes; verified by
    converting a synthetic manifest: plan step params include
    "same_net_escapes": 2.
No other new flags/params in range (checked: git diff over CLI mains for
add_argument; engine signatures for new kwargs).

Everything else in range is engine-internal (shared by both fronts for free)
or GUI-only:
  * #129 deferral/connect + scoped re-escape, #349 plane-pad strapping +
    repair strap-scale distant_radius, #351 region-join perf, #360 tap via
    clamp, #357 diff-pair validator fixes, #356 rotated-pad kernels + qfn
    own-pad scan -- ALL inside generate_bga_fanout / create_plane /
    route_disconnected_planes.route_planes / generate_qfn_fanout / shared
    modules. No new CLI-main post-pass (Class-2 clean).
  * route_planes/create_plane gained progress_callback/cancel_check kwargs
    (#364) -- runtime plumbing, default None, CLI passes nothing, GUI passes
    callables; deliberately NOT Claude-settable (no CLI flag). route.py's
    _dump_engine_config now skips them by name so the kwargs-diff harness
    doesn't see phantom keys.
  * GUI-only: Claude-tab Stop/cancel UX (#364 bc48cce/2d566c9), plan executor
    optimize_caps inheriting BGA options (dc771db -- deliberate exception to
    the per-step reset).
  * generate_underpad_escape gained skip_pad_keys (internal; no GUI call site
    -- GUI reaches underpad only through generate_bga_fanout).

GATES (all green):
  * test_manifest_plan_parity.py: 6201 flag-checks across 157 manifests, 0
    mismatches.
  * test_cli_postpass_coverage.py: 4 registered, 6 in active CLI use, 0
    failures.
  * test_gui_engine_parity.py (KiCad python, rp2350 chain): VERDICT PARITY --
    CLI and GUI both fully connected, 0 DRC, 0 kicad-unconnected; copper sets
    differ only by the documented A* tie-break class.

Still not swept (carried over from 8b41e5a): the diff-pair tab beyond what
the rp2350/#362 chain exercises, and the fanout tab's non-plane advanced
knobs. Nothing in this range touched their GUI layers.
