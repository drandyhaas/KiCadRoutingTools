f79743afb01788eced8bbb590ad0159860d31b52 2026-07-09 audited 280009b..HEAD (8 commits); 0 gaps needing fixes.
- The range is dominated by shared-engine Rust work that rides both fronts for free:
  the single-ended A* NodeMap fold (2b3f881, 0.17.2), the mimalloc global allocator
  (cfc982e, 0.17.3, lib.rs one-liner), and the PoseRouter PoseNodeMap fold (f79743a,
  0.17.4). All three are internal memory/cache refactors verified byte-identical --
  NO PyO3 signature / pub-fn / #[new] change (grep of the router.rs+pose_router.rs
  diff for those is empty), so the GUI's grid_router call sites need nothing new.
- diff_pair_routing.py (953db5b, revert of the #341 full_pose_budget cap): removes an
  internal throttle inside the private _route_direct_coupled_middle only -- no change to
  batch_route_diff_pairs or any public engine signature/param, so both fronts (CLI
  route_diff.py + GUI differential_gui) inherit it automatically via the shared call.
- CLI-only by design: memory_debug.py memory tooling (f537ce6) is a debug/profiling path,
  not a routing engine surface.
- GUI-only by design, no CLI counterpart needed:
  * 31e66d9 (claude_plan.py _write_drc_floors): plan-end floor writes now go through the
    existing GUI helper gui_utils.update_live_drc_floors (same board-scan min-merge as the
    per-step path) instead of raw pcbnew DesignSettings assignments that raised floors back
    above fine copper. Operates on a LIVE pcbnew board; the CLI has no live board -- it
    writes .kicad_pro floors via its own already-min-merged clearance_ledger path.
  * 4df8b86 (differential_gui / planes_gui / swig_gui): async busy-signal race fix (busy
    stays true until results are APPLIED). Purely a GUI threading concern; the CLI is
    synchronous and has no equivalent apply race.
- No new engine parameter, flag, or results-data key was added anywhere in the range, so
  there is nothing new to thread through the argparse layer, the GUI call sites, the config
  dict, the options panels, or settings_persistence.py.
- Static audit only (per CLAUDE.md method): git-log + engine-vs-GUI diff review. The live
  gui_parity harness was NOT run this pass -- it routes a full board inside KiCad's python
  and would have competed for RAM on the 8GB box with a concurrent stress wave whose timing
  measurement had to stay clean; unnecessary here since the range adds no engine params.
