# Independent final-placement review for issue #961

Reviewer 3 owns detached `KRT-961-review-final`; production/integration edits belong to the primary agent. No original fixture or parent branch was edited.

## Integrated baseline and method

Integrated baseline: `3f343981ae3fdbc82a60c0d41d693a03ccca98de`.
The reviewer independently read issue #961 and its expanded comment, parent PRs #968/#969/#970, their evidence and stated coverage boundaries, and repository instructions.

`verify_geometry_cli.py` creates copies of public esp_prog and applies only USB1 X translations through the actual writer. Native KiCad 10.0.0 reads the saved boards; expected body positions come from F.Fab PCB_SHAPE vertices and copper gaps from native effective F.Cu polygons. Neither expected measurement uses the new production connector geometry helper. Six controls cover X changes 0, -0.1, -1.0, -1.45, -2.1 and +0.4 mm. The positive, zero-minimum, legal-body and explicit-setback intent documents are held byte-identical while copper and board-edge flags vary. The source is SHA256 `165302e6a4f7aacdd64b3174df27ed8ddb19fd5f1f7effadd8d92205e25a120e`; it has no original project, rule or brief sibling, which is disclosed rather than synthesized into a claim about the source.

Baseline run `geometry-20260914-113819` completed 96 real `check_floorplan` and six real `check_drc` commands, preserving source and intent identities. The discarded first attempt omitted required `kind` from the harness-authored intent; its argparse refusal is not treated as a behavioral result. The corrected harness supplies the current schema, kind and units.

Independent native arithmetic confirms body overhang 0, 0.1, 1.0, 1.45, 2.1 and 0 mm respectively; +0.4 has setback 0.4 mm. West copper gaps are 1.6, 1.5, 0.6, 0.15, -0.5 and 2.0 mm. Bounds are (114,91,145.75,105.5) mm.

On the integrated baseline, -1.45 incorrectly satisfies the positive .05-.20 band with clearance .25 and edge .25, and fails with reported overhang .40 at edge .55. Edge zero still has effective margin .25. Native geometry is identical in every sweep arm. The legitimate -0.1 body overhang incorrectly fails the positive minimum because the baseline uses its inboard copper box. Explicit west intent without an inferred class also falsely reports unchanged USB1 nearest south.

Actual DRC uses the requested `--check-pad-edge --board-edge-clearance 0.25 --clearance-margin 0`. Its omitted copper-clearance resolution is .20 mm; that is distinct from the explicit .25/.55 floorplan sweep. Source has zero findings. -1.0 has no pad-edge findings but five pad-pad findings against fiducial local clearance 1.016 mm. -1.45 has two pad-edge findings, each .100 mm, plus five pad-pad findings (two contact). +.4 has one collateral pad-pad violation. These are controlled geometry examples, not otherwise-clean recommended placements.

The inherited native CLI/provenance harness also ran on this integrated baseline: 96 behavioral assertions passed. It verifies improving-pile use without force while `no_worse=true, legal=false`, exact poses/rotations, final SHA/lock/face ledger reconciliation, audits, strict search, multi-part atomic refusals, baseline freezing and requirement siblings. This baseline replay establishes the integrated parents' behavior before issue edits; it is not substituted for final testing.

## Scope and limitations

A successful provenance audit proves recorded authorship reconciliation within the cooperating filesystem transaction protocol. It does not certify engineering cleanliness. `place_pose --strict-legal` declares its measured pad/hole/outline scope and does not take a floorplan intent; actual declared mechanical intent is separately graded by `check_floorplan`. No full native board DRC, routed connectivity, model-quality or improved PCB-quality claim follows from these placement checks. The #969 cap-engine fixture has no BGA capacitors to move: it verifies actual delivered pricing (.25 omitted placement versus .10 routing), not improved placement geometry.

## Final verification and independently found corrections

The full behavioral rerun is `e182bbfe4c3ec19ad65999de9ff023dc84035128`. Final checked code/test/help revision is `9e65e44b0cb1bc7ab2b8656a48f4848ddbc38100`. All placement, routing, plugin, Rust and original fixture trees are identical between them. The only tool-runtime-source difference is `check_floorplan` help text: an AST comparison excluding the `help` keyword values proves the grading code is identical. The corrected real `--help`, new actual-CLI publication test and the native accepted/refused/dry publication witness were independently rerun at the latter revision. The intermediate `2448498c` changes only tests and adds the frozen inherited edge declarations. I inspected those diffs: original public boards and body-less fixture constants remain; added supported controls are explicit; expectation changes distinguish physical geometry from the former occupancy currency. See `final-checks/identity-and-checks.json`.

- `geometry/results.json` and `geometry/independent-assertions.json`: 96 real floorplan grades and six actual pad-edge DRC commands, with **1,543 independent assertions passed, zero failed**. Each final control board is byte-identical to its integrated-baseline counterpart. Native body overhang/setback and all-edge/declared-edge copper gaps, declared limits, units, basis, requirement source, disposition, effective occupancy margin and physical zero margin are checked. -0.1 now satisfies the positive band; -1.45 measures 1.45 at every clearance; a zero minimum leaves +0.4 setback unconstrained unless explicitly declared. The 1.0/1.45 legal-body controls independently pass/fail the copper edge channel. They retain the collateral pad-pair violations disclosed above.
- `parent-checks/commands.json`: **12 commands passed** at the final behavioral revision. The native parent publication harnesses passed **124 assertions across 41 actual CLI invocations**, including strict snap, improving-pile use without force, direct/coordinated rotations and faces, baseline identity/freezing, sibling preservation, final native pose/lock/face/SHA reconciliation, atomic refusal, seed/reconstruct/route-loop output and provenance audits. The #970 transaction tests and #968 edge/strict-search/native-pad tests pass. #969 native readers pass **224 assertions** on two-/four-layer boards; the actual cap engine retains omitted placement .25 versus routing .10 and explicit override controls. Both repository routing parity gates pass. These are reruns, not merely inherited parent reports.
- `publication/results.json` and `publication/accepted.kicad_pcb`: caller-selected USB1 [117.4,100,180] is published without force by `place_pose --strict-legal --clearance .2 --board-edge-clearance .25`. Native readback and final ledger reconcile exactly. The declared .05-.20 overhang plus max_setback=0 now grades successfully; the same geometry failed on the integrated baseline. The requested pad-edge DRC finds zero violations. The -1.45 trial refuses WORSE; dry -0.1 publishes nothing and adds no row.
- `publication-gap-before/` records a material issue found independently during review: even after geometry correction, supplied missing-body intent let seed repair/main write output with exit4, and reconstruct classify publish with exit0, status=ok and complete=true. The real written boards graded incomplete/error, yet their audits were CLEAN. The primary added staging and final connector acceptance before publication. `refusals/` verifies all three actual CLI paths against fresh and pre-existing destinations: six correct refusals, no committed candidate rows, no changed destination/input/intent bytes.
- `gates/`: eight actual seed/reconstruct controls verify accepted final boards, dry runs, in-place baseline freezing, real Windows read-only failures, requirement siblings and final native/ledger/audit agreement. Accepted outputs have exactly one final row. Dry output explicitly says status=dry_run, complete=false, engineering_clean=false and connector accepted=null with a reason. I also found the first wrapper's read-only failures emitted a raw traceback and no JSON; the corrected paths report unchanged with no row.
- `recovery/`: six independent actual-entrypoint fault/authorization cases pass. A failed board replacement plus denied project restoration reports **partial**, retains old-byte backups and forces audit UNPROVEN. Cleanup failure after committed publication reports **committed/published=true**, preserves the accurate final row, and retains the marker so audit remains UNPROVEN. Undeclared armed destinations refuse unchanged with zero rows. Recovery evidence and journals are retained; no manual reconciliation or marker deletion was used to obtain CLEAN.
- `unmeasured/`: native removal of USB1 Fab/Silk shapes preserves all nine pads. Both normal and explicit `--exit-zero` floorplan calls retain null body measurements, a missing-body reason, incomplete/failed verdict, and independently passing copper measurements. Exit zero does not turn that into a certificate.
- `exploratory/`: real stage_unaided pile plus binding valid USB1 intent retains 83 pad conflicts and reports exit4/status=exploratory/engineering_clean=false. One final row and a CLEAN provenance audit coexist with that disclosed unclean geometry. This preserves exploration rather than falsely calling the board clean.

Exact argument vectors, effective parameters, input/output/intent/project identities, grade evidence, native observations, ledger rows and audit outputs accompany the named JSON/log files. Original fixtures were preserved. The complete local runtime remains in the isolated reviewer worktree; this committed selection omits duplicate fixture copies and intermediate failed-harness artifacts, not failed product findings.

Reproduce with the bundled KiCad Python interpreter shown in the recorded command vectors. The standalone scripts discover the checkout root from their own location and create fresh timestamped private output directories. `verify_geometry_cli.py` works at either baseline or final checkout; compare its two output directories with `assert_geometry.py FINAL BASELINE`. `verify_unmeasured.py` creates the copied missing-body input; pass its board and intent paths to `verify_candidate_publication.py BOARD INTENT --expect-refused`. `verify_gate_publication.py`, `verify_gate_recovery.py`, `verify_publication.py`, `verify_exploratory.py` and `run_parent_checks.py` are standalone. The fault tests intentionally retain recovery artifacts in their private output directories.

No unresolved discrepancy remains within the exercised scope. The physical contract deliberately returns unmeasured for concave/cutout/nonrectangular boundaries, open/curved/concave bodies and unsupported faces rather than certifying them. Reviewer2's separate geometry evidence covers supported fallback/rotations/faces and adversarial geometry. The full repository suite, full native routing-chain DRC/connectivity and model-performance tests were not run by this reviewer. The tests do not establish higher PCB quality or model performance. A final documentation-only publication identity check follows this report; its result is recorded with the PR.
